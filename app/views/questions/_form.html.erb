<%= simple_form_for @question, html: { data: { turbo: false } } do |f| %>
  <%= f.text_area :content, id: "question_content", name: "question[content]", required: true %>
  <div class="submit-question">
    <%= f.button :submit, 'Ask', class: 'submit-btn' %>
  </div>
<% end %>
